/**
 *    author:  MaGnsi0
 *    created: 27.03.2023 22:22:06
**/
#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    int x, y, z;
    cin >> x >> y >> z;
    cout << (x <= y && y <= z ? "Good job!" : "Try again!");
}
